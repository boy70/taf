// Add these fields to the existing startup model to enable organization profiles

model startupProfile {
  id                String   @id @default(cuid())
  startupId         String   @unique
  profileImageUrl   String?
  coverImageUrl     String?
  bio               String?  @db.Text
  contactEmail      String?
  contactPhone      String?
  location          String?
  website           String?
  galleryImagesJson Json?    // Array of image URLs
  isPublic          Boolean  @default(true)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  startup startup @relation(fields: [startupId], references: [id], onDelete: Cascade)

  @@index([startupId])
}

model organizationPost {
  id          String   @id @default(cuid())
  startupId   String
  title       String
  content     String   @db.Text
  imageUrl    String?
  authorId    String
  status      String   @default("published") // published, draft
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  startup startup @relation(fields: [startupId], references: [id], onDelete: Cascade)
  author  user    @relation(fields: [authorId], references: [id])

  @@index([startupId])
  @@index([authorId])
}

model eventRegistration {
  id        String   @id @default(cuid())
  eventId   String
  userId    String
  status    String   @default("registered") // registered, attended, cancelled
  appliedAt DateTime @default(now())

  event event @relation(fields: [eventId], references: [id], onDelete: Cascade)
  user  user  @relation(fields: [userId], references: [id])

  @@unique([eventId, userId])
  @@index([userId])
}

// Update existing models with relationships
// startup model should have these additions:
// startupProfile     startupProfile?
// posts              organizationPost[]
// eventRegistrations eventRegistration[]

// event model should have:
// registrations eventRegistration[]

// user model should have:
// posts organizationPost[]
// eventRegistrations eventRegistration[]
